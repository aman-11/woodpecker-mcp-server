{
  "name": "mcp-pipeline-server",
  "version": "0.0.1",
  "description": "mcp-pipeline-server MCP server",
  "type": "module",
  "bin": {
    "mcp-pipeline-server": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && mcp-build",
    "watch": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "js-base64": "^3.7.7",
    "mcp-framework": "^0.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.19.0"
  }
}